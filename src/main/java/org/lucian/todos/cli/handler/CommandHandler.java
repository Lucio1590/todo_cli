package org.lucian.todos.cli.handler;

/**
 * Base interface for CLI command handlers.
 * Defines common operations that command handlers should support.
 * 

 */
public interface CommandHandler {
    
    // This interface serves as a marker and can be extended in the future
    // with common command handling methods if needed
    
}
